services:
  api:
    build: ./api
    ports: ["8000:8000"]
    env_file: .env
    volumes:
      - api_data:/data
      - ./web-admin/dist:/app/static

  agent:
    build: ./agent
    env_file: .env

volumes:
  api_data:
